{% extends 'base.html.twig' %}

{% block body %}
    <h1>Supprimer la publication</h1>

    <p>Voulez-vous vraiment supprimer la publication "{{ publication.title }}" ?</p>

    <a href="{{ path('publication_delete_confirm', {'id': publication.id}) }}" class="btn btn-danger">Supprimer</a>
    <a href="{{ path('publication_index') }}" class="btn btn-primary">Annuler</a>
{% endblock %}
