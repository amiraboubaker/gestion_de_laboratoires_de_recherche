{% extends 'base.html.twig' %}

{% block body %}
    <h1>Creer un nouvel equipement</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Creer</button>
    {{ form_end(form) }}
{% endblock %}