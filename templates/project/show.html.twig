{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ project.title }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Titre</th>
                <td>{{ project.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ project.description }}</td>
            </tr>
            <tr>
                <th>Date de début</th>
                <td>{{ project.startDate }}</td>
            </tr>
            <tr>
                <th>Date de fin</th>
                <td>{{ project.endDate }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ project.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('project_edit', {'id': project.id}) }}" class="btn btn-primary">Modifier</a>
    <a href="{{ path('project_delete', {'id': project.id}) }}" class="btn btn-danger">Supprimer</a>
{% endblock %}
